# Keep the Umi dev server off the backend port during local development.
PORT=5173

# Proxy /api requests from the Umi dev server to the backend.
API_PROXY_TARGET=http://localhost:8000

# API base URL baked into production bundles.
UMI_APP_API_BASE_URL=/api/v1
